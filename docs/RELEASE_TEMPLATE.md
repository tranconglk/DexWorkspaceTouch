# DexWorkspaceTouch Release Notes

- Version / versionCode:
- Build timestamp UTC:
- Changes:
- Compatibility:
- Known issues:
- APK SHA-256:
- Signing certificate SHA-256:
- Backend requirements:

## Update instructions

Install the new APK over the existing DexWorkspaceTouch installation. Do not uninstall first:
uninstalling removes application data and the device-local identity. For major releases, export
important workspaces first when the existing export function is available.
