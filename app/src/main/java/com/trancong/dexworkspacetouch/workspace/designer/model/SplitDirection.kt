package com.trancong.dexworkspacetouch.workspace.designer.model

enum class SplitDirection {
    /** Splits a cell into top and bottom cells. */
    HORIZONTAL,

    /** Splits a cell into left and right cells. */
    VERTICAL,
}
